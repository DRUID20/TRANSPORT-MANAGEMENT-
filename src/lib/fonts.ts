import { GeistSans } from "geist/font/sans";
import { GeistMono } from "geist/font/mono";

/**
 * Project fonts — Geist Sans + Geist Mono (Vercel, OFL).
 *
 * Industry-standard for premium SaaS in 2026 (used by Vercel, Linear,
 * Resend, and most Vercel-template projects). Optical balance between
 * UI density and editorial moments, and the matching mono pairs
 * cleanly for tabular numerics on the dashboard + tables.
 */
export const fontSans = GeistSans;
export const fontMono = GeistMono;
